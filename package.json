{
  "name": "stream-porch",
  "version": "0.1.0",
  "description": "Experimenting with websockets to see if I can make something with similar functionality to a stream deck",
  "main": "index.js",
  "scripts": {
    "forward": "npx localtunnel --port 9000 --subdomain streamporch",
    "serve": "node index.js",
    "start": "npm-run-all --parallel serve forward",
    "test": "echo YOLO"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.2.0",
    "eslint-config-personal": "github:veryGoodDev/eslint-config-personal",
    "npm-run-all": "^4.1.5"
  },
  "eslintConfig": {
    "extends": [
      "eslint-config-personal"
    ]
  }
}
